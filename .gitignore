# 依赖与构建产物
node_modules/
.next/
out/

# 运行时数据（上传的数据集落盘目录）
.data/

# 环境变量（含密钥，勿提交）
.env
.env.local

# 编辑器/系统
.DS_Store
*.log

# 运行时日志（dev 启动器轮转产生 dev-server.log / .prev，勿入库）
logs/

# TypeScript 增量构建缓存
tsconfig.tsbuildinfo

# 项目记忆（workbuddy 工作区记忆，仅本地 working tree 使用）
.workbuddy/
